<%= form_with(model: @comment, local: true, url: url) do |form| %>
<% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@comment.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @comment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>
<%= form.label :text_comments %><br>
<%= form.text_field :text_comments %>
<%= form.hidden_field :user_id, value: params[:user_id] %>
<%= form.hidden_field :post_id, value: @comment.new_record? ? params[:id] : params[:post_id] %>
<%= form.submit %><br> 
<% end %>    