<%= form_with(model: state, local: true) do |form| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :country_id %>
    <%= form.select :country_id, @countries.collect {|c| [c.name, c.id]}, { include_blank: true } %>
    <%#= form.collection_select :country_id, @countries, :id, :name, { prompt: true } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
